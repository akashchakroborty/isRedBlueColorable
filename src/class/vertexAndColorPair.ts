class VertexAndColorPair {
  vertex: string;

  vertexColor: number;

  constructor(vertex: string, vertexColor: number) {
    this.vertex = vertex;
    this.vertexColor = vertexColor;
  }
}

export default VertexAndColorPair;
